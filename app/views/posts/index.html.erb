<h1>Listing posts</h1>

<table>
  <tr>
    <th>Content</th>
    <th>Is approved</th>
    <th>Topic</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%= post.content %></td>
    <td><%= post.is_approved %></td>
    <td><%= post.topic %></td>
    <td><%= post.user %></td>

  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Post', new_topic_post_path %>
