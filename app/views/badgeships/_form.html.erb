<%= simple_form_for(@badgeship) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :user %>
    <%= f.association :badge %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
